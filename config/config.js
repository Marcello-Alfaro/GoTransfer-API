export const SALT = 12;
export const MAX_FILE_SIZE = 6 * 1024 ** 3;
export const {
  PORT,
  JWT_SECRET,
  SENDGRID_API_KEY,
  FROM_EMAIL,
  API_URL,
  DB_DATABASE,
  DB_USER,
  DB_PASS,
  DB_HOST,
  DB_DIALECT,
  CORS_ORIGIN,
} = process.env;
